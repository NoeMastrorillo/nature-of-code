# nature-of-code

p5.js sketches from my reading of Daniel Shiffman's book *The Nature Of Code*.

To run a sketch, execute one of the following commands, then follow the sketch's path (or run from the sketch's folder).

```python
# Using Python
python -m http.server 8000

# Using Node.js
npx http-server
```
